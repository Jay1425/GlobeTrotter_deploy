# Environment Variables for GlobeTrotter Deployment
# Copy this file to .env for local development
# For Render.com deployment, set these in the dashboard

# Flask Configuration
SECRET_KEY=your-super-secret-key-change-this
FLASK_ENV=production

# Database (Render will auto-populate DATABASE_URL)
DATABASE_URL=postgresql://username:password@host:5432/database

# Email Configuration (Gmail SMTP)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Google OAuth (Get from Google Cloud Console)
# IMPORTANT: Never commit these values to git!
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Python Version (for Render.com)
PYTHON_VERSION=3.11.9

# SECURITY NOTE:
# 1. Create a .env file with your actual values (already in .gitignore)
# 2. Never commit .env file or hardcode secrets
# 3. On Render, set these in Environment Variables tab
# 4. If secrets are exposed, regenerate them immediately
